﻿@{
    ViewBag.Title = "Tạo danh mục mới";
    Layout = "~/Views/Shared/_Layout_admin.cshtml";
}

<div class="content-page">
    <div class="container-fluid">
        <h2>Tạo danh mục mới</h2>

        <form id="createForm" method="post">
            <div class="form-group">
                <label for="Name" class="control-label">Tên danh mục <span class="text-danger">*</span></label>
                <input type="text" id="Name" name="Name" class="form-control form-control-sm" maxlength="100" required />
                <span id="nameError" class="text-danger" style="display: none;"></span>
            </div>

            <div class="form-group">
                <label for="Description" class="control-label">Mô tả</label>
                <textarea id="Description" name="Description" class="form-control form-control-sm" rows="4"></textarea>
                <span id="descriptionError" class="text-danger" style="display: none;"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Tạo mới</button>
                <button type="reset" class="btn btn-secondary" onclick="resetForm()">Nhập lại</button>
                <a href="/Categories/Index" class="btn btn-info">Quay về danh sách</a>
            </div>
        </form>
    </div>
</div>

<script>
    $(document).ready(function () {
        // Validation client-side
        function validateForm() {
            var isValid = true;
            var name = $("#Name").val().trim();
            var description = $("#Description").val().trim();

            // Reset errors
            $("#nameError").hide().text("");
            $("#descriptionError").hide().text("");

            // Validate Name
            if (!name) {
                $("#nameError").text("Tên danh mục không được để trống.").show();
                isValid = false;
            } else if (name.length > 100) {
                $("#nameError").text("Tên danh mục không được vượt quá 100 ký tự.").show();
                isValid = false;
            }

            return isValid;
        }

        // Submit form via AJAX
        $("#createForm").submit(function (e) {
            e.preventDefault();

            // Validate
            if (!validateForm()) {
                return;
            }

            // Get form data
            var formData = {
                Name: $("#Name").val().trim(),
                Description: $("#Description").val().trim()
            };

            // Disable submit button
            var $submitBtn = $(this).find('button[type="submit"]');
            var originalText = $submitBtn.text();
            $submitBtn.prop('disabled', true).text('Đang xử lý...');

            // AJAX request
            $.ajax({
                url: '/Categories/CreateCategory',
                type: 'POST',
                data: formData,
                dataType: 'json',
                success: function (result) {
                    console.log(result);
                    if (result && result.success) {
                        alert(result.message || 'Tạo danh mục thành công!');
                        window.location.href = '/Categories/Index';
                    } else {
                        // Hiển thị lỗi từ server
                        if (result && result.errors && result.errors.length > 0) {
                            var errorMsg = (result.message || 'Có lỗi') + '\n\n' + result.errors.join('\n');
                            alert(errorMsg);

                            // Hiển thị lỗi vào các field tương ứng nếu có
                            if (result.errors.some(e => e.toLowerCase().includes('name'))) {
                                $("#nameError").text(result.errors.find(e => e.toLowerCase().includes('name'))).show();
                            }
                        } else {
                            alert(result.message || 'Có lỗi khi tạo danh mục.');
                        }
                    }
                },
                error: function (xhr, status, error) {
                    console.log('XHR Error:', xhr.responseText);
                    var errorMessage = 'Lỗi kết nối hoặc server: ' + status;
                    try {
                        var errorResponse = JSON.parse(xhr.responseText);
                        if (errorResponse.message) {
                            errorMessage = errorResponse.message;
                        }
                    } catch (e) {
                        // Ignore parse error
                    }
                    alert(errorMessage);
                },
                complete: function () {
                    // Re-enable submit button
                    $submitBtn.prop('disabled', false).text(originalText);
                }
            });
        });

        // Real-time validation
        $("#Name").on('blur', function () {
            var name = $(this).val().trim();
            if (!name) {
                $("#nameError").text("Tên danh mục không được để trống.").show();
            } else if (name.length > 100) {
                $("#nameError").text("Tên danh mục không được vượt quá 100 ký tự.").show();
            } else {
                $("#nameError").hide();
            }
        });

        $("#Name").on('input', function () {
            if ($("#nameError").is(':visible')) {
                var name = $(this).val().trim();
                if (name && name.length <= 100) {
                    $("#nameError").hide();
                }
            }
        });
    });

    function resetForm() {
        $("#createForm")[0].reset();
        $("#nameError, #descriptionError").hide().text("");
    }
</script>